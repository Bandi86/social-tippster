# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Environment files
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example
frontend/.env.local
frontend/.env.*
!frontend/.env.example

# Build outputs
dist/
build/
backend/dist/
frontend/.next/
frontend/out/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage
coverage/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Docker
docker-compose.override.yml

# Uploads
uploads/

temp/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Do not ignore test, debug, or doc files, but keep them out of root
!docs/
!docs/debug/
!tests/
!tests/images/
!CLEANUP_SUMMARY.md
!cookies.txt
!debug-*.png
!check-admin-status.js

# Never ignore or delete cookies.txt, but it must be in docs/
# Never ignore or delete CLEANUP_SUMMARY.md, but it must be in docs/
# Never ignore or delete debug PNGs or debug scripts, but they must be in docs/debug/
# Never ignore or delete test files, but they must be in tests/
# Never ignore or delete documentation files, but they must be in docs/
# Keep the root directory clean: no test, image, debug, or documentation files should be present in the root.
